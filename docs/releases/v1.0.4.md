# Release v1.0.4

**Fecha**: 2026-02-14

## 🔧 Correcciones Críticas de Base de Datos y Electron

### Solucionado
- **[CRÍTICO]** Corregido error 500 en `/api/institucion` debido a ruta de base de datos incorrecta. Prisma CLI resolvía las rutas mal en producción. Se actualizó la variable de entorno y ubicación del archivo DB.
- **[CRÍTICO]** Corregido fallo de inicio de Electron (`TypeError: Cannot read properties of undefined (reading 'isPackaged')`). Los scripts de lanzamiento `launcher.js` ahora eliminan explícitamente la variable `ELECTRON_RUN_AS_NODE=1` antes de iniciar el proceso.
